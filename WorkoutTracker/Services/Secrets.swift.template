// Secrets.swift.template
// Copy this file to Secrets.swift and fill in your Supabase project values.
// Secrets.swift is gitignored — never commit your real keys.

enum Secrets {
    static let supabaseURL = "https://<your-project-ref>.supabase.co"
    static let supabaseKey = "<your-supabase-anon-key>"
}
